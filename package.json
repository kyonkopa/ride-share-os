{
  "name": "ked-transport-services-os",
  "version": "1.0.0",
  "private": true,
  "description": "KED Transport Services OS - Full Stack Application",
  "scripts": {
    "postinstall": "cd frontend && yarn install && yarn build",
    "start": "cd backend && bundle exec rails server -p $PORT -e production"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
